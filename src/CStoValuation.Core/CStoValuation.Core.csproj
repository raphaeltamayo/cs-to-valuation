<Project Sdk="Microsoft.NET.Sdk">

  <!-- Shared policy (nullable, implicit usings, warnings-as-errors) lives in
       Directory.Build.props. Core has no dependencies — that is the point of it. -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

</Project>
