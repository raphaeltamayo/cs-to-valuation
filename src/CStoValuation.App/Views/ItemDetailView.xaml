<UserControl x:Class="CStoValuation.App.Views.ItemDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behaviors="clr-namespace:CStoValuation.App.Behaviors"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:vm="clr-namespace:CStoValuation.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ItemDetailViewModel}">

    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="10" Padding="16">
        <Grid>
            <!-- Empty state -->
            <TextBlock Text="Select an item to see its prices and history."
                       Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"
                       VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasSelection}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Detail content -->
            <StackPanel Visibility="{Binding HasSelection, Converter={StaticResource BoolToVisibility}}">
                <StackPanel Orientation="Horizontal">
                    <Image behaviors:ImageLoader.SourceUrl="{Binding ImageUrl}"
                           Width="84" Height="63" Stretch="Uniform" VerticalAlignment="Top" />
                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold"
                               TextWrapping="Wrap" Margin="12,0,0,0" Width="220" />
                </StackPanel>

                <!-- Price sources -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Skinport (gross)"
                               Foreground="{StaticResource MutedTextBrush}" FontSize="12" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SkinportGrossText}"
                               HorizontalAlignment="Right" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Skinport (net of fees)"
                               Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,6,0,0" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SkinportNetText}"
                               HorizontalAlignment="Right" Margin="0,6,0,0"
                               Foreground="{StaticResource PositiveBrush}" FontWeight="SemiBold" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Steam Market (gross · volume)"
                               Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,6,0,0" />
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal"
                                HorizontalAlignment="Right" Margin="0,6,0,0">
                        <TextBlock Text="{Binding SteamPriceText}" />
                        <TextBlock Text=" · " Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding SteamVolumeText}" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="PriceEmpire (gross)"
                               Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,6,0,0"
                               Visibility="{Binding IsPriceEmpireEnabled, Converter={StaticResource BoolToVisibility}}" />
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding PriceEmpireGrossText}"
                               HorizontalAlignment="Right" Margin="0,6,0,0"
                               Visibility="{Binding IsPriceEmpireEnabled, Converter={StaticResource BoolToVisibility}}" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="CSFloat (gross)"
                               Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,6,0,0"
                               Visibility="{Binding IsCsFloatEnabled, Converter={StaticResource BoolToVisibility}}" />
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding CsFloatPriceText}"
                               HorizontalAlignment="Right" Margin="0,6,0,0"
                               Visibility="{Binding IsCsFloatEnabled, Converter={StaticResource BoolToVisibility}}" />
                </Grid>

                <!-- Price history chart -->
                <Grid Margin="0,18,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="PRICE TREND" FontSize="11" VerticalAlignment="Center"
                               Foreground="{StaticResource MutedTextBrush}" />
                    <ComboBox Grid.Column="1" Width="72"
                              ItemsSource="{Binding Ranges}"
                              SelectedItem="{Binding SelectedRange}"
                              ToolTip="Chart look-back window (uses Steam daily history when signed in)" />
                </Grid>

                <Grid Height="200">
                    <lvc:CartesianChart x:Name="TrendChart"
                                        Series="{Binding Series}"
                                        XAxes="{Binding XAxes}"
                                        YAxes="{Binding YAxes}"
                                        Background="Transparent"
                                        Visibility="{Binding HasHistory, Converter={StaticResource BoolToVisibility}}" />
                    <TextBlock Text="No recent sales data to chart for this item."
                               Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"
                               VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasHistory}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
